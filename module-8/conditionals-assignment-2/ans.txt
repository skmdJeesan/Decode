Predict the output

1. code:
#include<iostream>
using namespace std;
int main( ) {
  int a = 5, b, c ;
  b = a = 15 ;
  c = a < 15 ;
  cout << "a = " << a << ", b = " << b << " , c = "<< c ;
  return 0;
}
output: 
a = 15, b = 15 , c = 0

2. code:
#include<iostream>
using namespace std;
int main() {
  int x = 3 ;
  float y = 3.0 ;
  if (x == y)
   cout <<"x and y are equal" ;
  else
   cout << "x and y are not equal" ;
  return 0;
}
output: 
x and y are equal

3. code:
#include<iostream>
using namespace std;
int main(){
  int test = 0;
  cout << "First character " << '1' << endl;
  cout << "Second character " << (test ? 3 : '1') << endl;
  return 0;
}
output:
First character 1
Second character 49
explaination: 49 is ascii of '1', 49 is the output because the conditional (?:) operator must produce one common type. since one operand is int, the char is promoted to int.

4. code:
#include <iostream>
using namespace std;
int main(){
  int a = 18; int b = 12;
  bool t = (a > 20 && b < 15) ? true : false;
  cout << "Value of t: " << t ;
  return 0;
}
output:
Value of t: 0


5. code:
#include <iostream>
using namespace std;
int main() {
  int number = -4;
  char result;
  result = number > 0 ? 'P' : 'N';
  cout << result << endl;
  return 0;
}
output: N